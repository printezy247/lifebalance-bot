# Do NOT pin httpx / httpcore / anyio here.
# python-telegram-bot pins its own compatible versions of those, and adding
# manual pins caused an unresolvable conflict that broke the Railway build
# (httpcore<1.0.0 vs httpx 0.25.2 requiring httpcore==1.*).
#
# Python version is pinned in .python-version — leaving it unpinned let the
# Railway builder drift to 3.13, which PTB 20.7 / APScheduler 3.10.4 predate.

python-telegram-bot[job-queue]>=21.6,<22
python-dotenv>=1.0,<2
requests>=2.32,<3
python-dateutil>=2.9,<3
# zoneinfo has no bundled tz database on slim Linux images.
tzdata>=2024.1
